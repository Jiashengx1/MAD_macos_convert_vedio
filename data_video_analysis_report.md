# data 视频样本分析报告

生成时间：2026-05-22  
分析范围：`data/` 下 5 个目录、11 个 `.mp4` 文件  
工具：当前项目的 `main.py` 探测逻辑、FFmpeg/FFprobe 8.1

## 结论摘要

`data/` 下 11 个文件虽然后缀都是 `.mp4`，但实际封装全部是 `MPEG-PS (MPEG-2 Program Stream)`，不是标准 MP4。macOS 的 QuickTime/AVFoundation 对 `.mp4` 容器较严格，遇到这种“假 MP4”加上异常时间戳时，容易直接无法打开或无声。

这些文件不是单一问题，而是多个兼容性问题叠加：

- 文件后缀是 `.mp4`，实际容器是 MPEG-PS。
- 多数文件存在巨大的音视频起始时间差，范围约 `5965s`、`11930s`、`15799s`、`83513s`。
- 2 个文件开头有异常字节，直接 ffprobe 无法得到有效视频尺寸，必须跳过前置字节后才能识别视频。
- 7 个文件视频是 HEVC/H.265、`2560x1440`、`yuvj420p`，如果目标是 H.264 1080p，则必须重新编码视频。
- 9 个文件音频是 `pcm_alaw` / G.711 A-law，8 kHz 单声道，不适合作为 macOS 兼容 MP4 的最终音频流，需要转 AAC。
- 4 个文件虽然已经是 H.264 1920x1080，但仍有 MPEG-PS、PCM A-law、异常时间戳、`yuvj420p` 等问题；视频 copy 试验可生成 MP4，但解码时出现大量 `non monotonically increasing dts` 警告，不建议作为默认路径。

因此，当前这 11 个样本全部应走 `full_transcode`。只做 remux 或只转音频不够稳。

## 分组分析

### 1. `6-19F东通道朝南` / `6-19F护士站3`

文件数：2  
共同特征：

- 容器：实际是 `mpeg` / MPEG-PS，不是 MP4。
- 视频：HEVC/H.265，`2560x1440`，`yuvj420p`。
- 音频：AAC，16 kHz，单声道。
- 音视频起始时间差：约 `5965s`。

这组音频本身已经是 AAC，但容器、视频编码、分辨率、像素格式和时间戳都不符合目标输出。由于目标强制 1920x1080，视频必须重新编码。

### 2. `3-2眼科谈话间`

文件数：5  
共同特征：

- 容器：实际是 MPEG-PS。
- 视频：HEVC/H.265，`2560x1440`，`yuvj420p`。
- 音频：`pcm_alaw`，8 kHz，单声道。
- 多数文件有严重音视频起始时间差：
  - 午间 2、3 段：约 `83513s`。
  - 傍晚 1、2 段：约 `11930s`。
- `...120000-140000_1.mp4` 直接解析时视频尺寸为 0，必须跳过 `4664` 字节后才能识别 2560x1440 视频；音频还需要从未跳过的原始输入读取。

这组必须完整转码：HEVC 转 H.264、2560x1440 降到 1920x1080、PCM A-law 转 AAC，并重建音视频 PTS。

### 3. `3号楼2楼眼科等待区`

文件数：4  
共同特征：

- 容器：实际是 MPEG-PS。
- 视频：H.264，`1920x1080`，`yuvj420p`。
- 音频：`pcm_alaw`，8 kHz，单声道。
- 2、3、4 段音视频起始时间差约 `15799s`。
- `...170000-182000_1.mp4` 直接解析时视频尺寸为 0，必须跳过 `3852` 字节后才能识别视频；音频需要从未跳过的原始输入读取。

这组表面上视频已经是 H.264 1080p，理论上最值得尝试“视频 copy + 音频转码”。我对第 4 段做了临时测试：

- 命令策略：`-c:v copy`，音频转 AAC，输出 MP4。
- 结果：能生成 MP4，且 ffprobe 显示标准 MP4、H.264、AAC、start_time 为 0。
- 风险：完整解码验证时出现大量 `Application provided invalid, non monotonically increasing dts`，并出现 H.264 解码块错误。

所以这组不建议默认视频 copy。完整转码能通过 `setpts=PTS-STARTPTS` 重建视频时间轴，兼容性更稳。

## 逐文件摘要

| # | 文件 | 直接识别视频 | skip bytes | 视频 | 音频 | A/V 起始差 | 当前建议 |
|---|---|---:|---:|---|---|---:|---|
| 1 | `3-2眼科谈话间_20260414110615/..._1.mp4` | 否 | 4664 | HEVC 2560x1440 yuvj420p | pcm_alaw 8k mono | 无法直接计算 | full_transcode |
| 2 | `3-2眼科谈话间_20260414110615/..._2.mp4` | 是 | 0 | HEVC 2560x1440 yuvj420p | pcm_alaw 8k mono | 83513.272s | full_transcode |
| 3 | `3-2眼科谈话间_20260414110615/..._3.mp4` | 是 | 0 | HEVC 2560x1440 yuvj420p | pcm_alaw 8k mono | 83513.271s | full_transcode |
| 4 | `3-2眼科谈话间_20260414110802/..._1.mp4` | 是 | 0 | HEVC 2560x1440 yuvj420p | pcm_alaw 8k mono | 11930.485s | full_transcode |
| 5 | `3-2眼科谈话间_20260414110802/..._2.mp4` | 是 | 0 | HEVC 2560x1440 yuvj420p | pcm_alaw 8k mono | 11930.443s | full_transcode |
| 6 | `3号楼2楼眼科等待区_20260414111944/..._1.mp4` | 否 | 3852 | H.264 1920x1080 yuvj420p | pcm_alaw 8k mono | 无法直接计算 | full_transcode |
| 7 | `3号楼2楼眼科等待区_20260414111944/..._2.mp4` | 是 | 0 | H.264 1920x1080 yuvj420p | pcm_alaw 8k mono | 15799.589s | full_transcode |
| 8 | `3号楼2楼眼科等待区_20260414111944/..._3.mp4` | 是 | 0 | H.264 1920x1080 yuvj420p | pcm_alaw 8k mono | 15799.509s | full_transcode |
| 9 | `3号楼2楼眼科等待区_20260414111944/..._4.mp4` | 是 | 0 | H.264 1920x1080 yuvj420p | pcm_alaw 8k mono | 15799.549s | full_transcode |
| 10 | `6-19F东通道朝南_20260521154247/..._1.mp4` | 是 | 0 | HEVC 2560x1440 yuvj420p | AAC 16k mono | 5965.311s | full_transcode |
| 11 | `6-19F护士站3_20260521154342/..._1.mp4` | 是 | 0 | HEVC 2560x1440 yuvj420p | AAC 16k mono | 5965.284s | full_transcode |

## 为什么 macOS 播放不了

macOS 看到 `.mp4` 后会按 MP4/QuickTime 容器预期解析，但这些文件实际是 MPEG-PS。Windows 上很多播放器或解码链更宽松，会扫描内容并容忍错误容器；macOS 自带播放器更依赖标准容器、索引和时间戳。

更具体地说：

- QuickTime 期望 MP4 的 `moov`、track、sample table 等结构，这批文件不是这种结构。
- 音频和视频的 PTS 起点差异巨大，播放器可能认为音频在几千秒甚至几万秒之后才开始。
- 部分文件开头有设备私有数据或异常字节，普通解析器一开始拿不到有效视频参数。
- `pcm_alaw` 适合监控/电话类原始音频，但不是通用 MP4 播放兼容音频。
- `yuvj420p` / full range 色彩标记不是唯一原因，但和错误封装、坏时间戳叠加后，会进一步降低兼容性。

## 对转换策略的建议

当前工具对这些文件选择完整转码是合理的。推荐最终输出保持：

- 容器：标准 MP4 / QuickTime MOV
- 视频：H.264 / `avc1`
- 分辨率：1920x1080
- 像素格式：`yuv420p`
- 音频：AAC，单声道，默认 16k
- 时间轴：视频和音频都从 0 开始

不建议把 `3号楼2楼眼科等待区` 这组默认改成“视频 copy + 音频转码”。虽然它们的视频本身是 H.264 1080p，但 copy 输出仍有非单调 DTS 问题。除非后续增加更严格的验证流程，例如输出后做完整 ffmpeg 解码检查且没有 DTS/PTS 警告，否则默认 copy 风险偏高。

## 对脚本逻辑的影响

现有脚本需要继续保留这些能力：

- 对 MPEG-PS 假 MP4 的识别。
- `skip_initial_bytes` 探测，至少覆盖本次发现的 `4664` 和 `3852` 字节前置异常。
- `use_unskipped_audio` 路径，因为部分文件的视频需要跳过前置字节，音频却只能从未跳过的输入读到。
- `setpts=PTS-STARTPTS` 和 `asetpts=PTS-STARTPTS`。
- `aresample=async=1:first_pts=0`，这批文件时间戳损坏明显，不建议删除。
- 完整转码后的 ffprobe 校验，最好继续保留完整解码检查作为最终质量验证。

